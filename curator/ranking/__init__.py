"""RankingAgent package — weighted composite ranking + output composer.

OWNER: Agent A5 (Stage S-05). Computes the weighted composite from
config/platform_weights.yml, ranks top-N with a rationale line, builds the
BotAgent output card (photo + rationale + 3 numbered variants), and handles
user selection. Stub only at S-00.
"""
